# 2026-04-07 Daily Log

## Freshdesk 分诊（凌晨继续）

### 00:00 分诊
- 处理 4 张工单（2 关闭 + 2 指派 Lena）
- #108297: Order #39764 超一月未收 → Lena
- #108295: Order #39286 Move 2月23日下单无物流 → Lena
- 之前 19:38 和 16:00 的分诊都已推送给用户

### 最终修复方案（08:37 确认生效）
- `announce` 模式根本不能用于飞书——需要 `contact:contact.base:readonly` 权限解析用户
- 直接调飞书 API 发消息不需要这个权限
- 方案：Python 脚本 `scripts/freshdesk-triage-push.py` 直接跑分诊 + 调飞书 API 推送
- Cron 改为 isolated session 跑 `python3 /root/.openclaw/workspace/scripts/freshdesk-triage-push.py`
- 用户确认收到 ✅

### 赋值失效问题修复（10:56）
- 12 张工单有 `ai-draft-ready` 标签但 `responder_id` 为 null
- 根因：Gwen/Lena/Jennifer/Jony 不在 Customer Support 组（`agents: []`）
- Freshdesk API 静默忽略对非组内 agent 的赋值，不报错
- 修复：通过 API `PUT /groups/150000248275` 添加 `agent_ids`，12 张工单全部重新赋值成功
- 以后分诊脚本也要确保组内有 agent

### 用户要求
1. 每 4 小时跑一次（cron: `0 */4 * * *`） ✅
2. 回溯窗口 6 小时，已处理的跳过（通过 ai-draft-ready/auto-spam-closed 标签） ✅
3. 报告要主动推送（改用 Python 脚本直接调飞书 API） ✅
4. 每个工单都要有逐条说明 + 起草回复 ✅

## Shopify 集成

### TRIAGE_RULES.md 更新
- 补充多轮对话必须看 conversation history
- 补充 `noreply@` 本地部分匹配规则（匹配 noreply@payoneer.com 等）

## Shopify 集成

### 凭据
- 域名: chessnutech.myshopify.com
- Client ID: [REDACTED]
- Secret: [REDACTED]
- **Access Token: [REDACTED]**（通过 OAuth 获取）
- Scope: read_customers, read_orders
- 已存入 `~/.openclaw/credentials/chessnut-services.json`

### Shopify API 测试结果
- ✅ 可以查订单：获取订单号、商品、客户邮箱、fulfillment 状态
- ✅ 可以获取物流单号和物流公司
- 示例：Order #41564 → 物流号 380241010355, 快递公司 other

### 未来分诊集成计划
- 遇到客户问订单状态时，直接用 Shopify API 查物流信息
- 获取 fulfillment 的 tracking_number 和 tracking_company
- 用 17track API 查物流轨迹，起草回复

## 17track 物流追踪

### API Key
- Token: [REDACTED]
- 已存入 `chessnut-services.json` 的 `track17` 字段

### API 测试结果
- ❌ 所有 API 端点都返回 `-18010013 "Submitted data is invalid"`
- 测试了 v2.1-v2.4 所有版本的 register、gettrackinfo 端点
- 用真实物流号 380241010355 也一样错误
- 排除格式问题，确认是 API Key 权限问题

### 原因
- 用户后台截图显示「实时接口」需联系 17track 开通
- 用户已联系客服，待开通后重新测试

### 用户的分诊需求（待实现）
- 首次遇到物流号 → 打 `waiting-for-tracking` 标签
- 每次分诊检查有此标签的工单 → 查询 17track 物流更新
- 有更新后 → 根据最新物流状态起草回复
- 还要从 Shopify API 获取物流号再查 17track

## 凭据管理
- 所有凭据已从 TOOLS.md 迁移到 `~/.openclaw/credentials/chessnut-services.json`
- TOOLS.md 清空了密钥，只保留引用
- TOOLS.md 推送到了 GitHub

## 用户偏好

### Freshdesk 分诊报告格式
- 用户确认当前报告格式好（逐条列出：工单号 → 指派人 + 标签、标题、摘要、起草回复片段）
- 以后分诊报告统一用这个格式

## GitHub 推送
- 已推送: TRIAGE_RULES.md, SKILL.md, FAQ_KNOWLEDGE_BASE.md, memory/*.md, TOOLS.md
- 多次提交，都成功推送到 master
